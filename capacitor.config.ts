import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PROG2005-Assessment3-InventoryApp-Ionic',
  webDir: 'www'
};

export default config;
